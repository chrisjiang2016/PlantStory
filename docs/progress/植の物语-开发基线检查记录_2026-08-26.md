# 植の物语 — 开发基线检查记录（2026-08-26）

> 检查时间：2026-08-26 16:15–16:24 GMT+8  
> 项目路径：`C:\Users\Chris J\.openclaw\workspace-fullstack-dev\plant-story`  
> 检查原则：以当前代码、Git、构建/测试、Docker 配置和线上端点实测为准；历史记录只作为辅助证据。

## 一、检查范围

- 历史记忆与 Sprint 交接记录
- `docs/progress/植の物语-任务进度.md`
- `plant-story/memory/` 中 2026-08-15 至 2026-08-19 的交接记录
- 当前 Git 分支、远端和提交历史
- NestJS 后端代码、路由、构建和测试
- Flutter 客户端 analyze、测试和 Web release build
- Dockerfile、Docker Compose、Nginx 和部署文档
- GitHub 仓库、GitHub Actions、GitHub/Vercel 部署状态
- Vercel 与 Railway 公网地址
- 敏感文件的 Git 跟踪状态

## 二、当前真实基线

### 1. Git 与仓库

- GitHub：`https://github.com/chrisjiang2016/PlantStory`
- 当前分支：`main`
- 本地 `HEAD` 与 `origin/main`：ahead 0 / behind 0
- 工作区：检查完成后保持干净
- 最新提交：`bdde66d fix: let Vercel CLI read token from environment`
- 最新提交时间：2026-08-19 13:13:18 +08:00

### 2. 后端

- 技术栈：NestJS 11、TypeScript、Prisma 6.19.3、PostgreSQL、JWT、Swagger。
- Prisma 当前 datasource：`provider = "postgresql"`。
- 业务模块：10 个。
- Controller HTTP 方法路由：44 条。
- `npm run build`：通过。
- 历史自动化基线：7 suites / 88 tests 曾通过。
- 当前 `npm test`：失败。原因不是业务断言失败，而是测试脚本仍传 `DATABASE_URL=file:./test-all.db`，与当前 PostgreSQL Prisma provider 不兼容：

```text
Error validating datasource `db`:
the URL must start with the protocol `postgresql://` or `postgres://`.
```

结论：PostgreSQL 迁移后，后端测试基础设施没有同步迁移，当前不能再宣称“88 tests 可重复通过”；必须先恢复 PostgreSQL 测试数据库基线。

### 3. Flutter 客户端

本次重新执行：

```text
flutter analyze              ✅ No issues found
flutter test                 ✅ 19 tests passed
flutter build web --release  ✅ Built build/web
```

- Auth/Garden/Reminders/Recognition 四条主链路代码已完成真实 Nest API 接入。
- Web Cookie Auth、401 自动刷新、启动恢复会话和登出清理已实现。
- Demo 模式已完成花园、提醒和本地示例识别闭环，并通过 localStorage 持久化。
- Web build 存在 `flutter_secure_storage_web` 的 Wasm dry-run 兼容性提示，不影响当前 JavaScript Web release 产物。
- Android/iOS 真机、相机权限和原生 Keychain/Keystore 仍未实测。

### 4. Docker / PostgreSQL

仓库实际已有：

- `backend/Dockerfile`
- `client/Dockerfile`
- `client/nginx.conf`
- `docker-compose.yml`
- `.env.example`
- `docs/deployment/Docker部署指南.md`

Compose 可解析三个服务：

```text
postgres
backend
frontend
```

2026-08-15 交接记录显示三服务曾完成本地构建、启动和健康检查。2026-08-26 本机 Docker Desktop daemon 未运行，因此本次未能重新执行容器健康检查。`docker-compose.yml` 另有 `version` 字段已废弃的非阻塞警告。

### 5. CI/CD 与公网状态

- 仓库已存在 `.github/workflows/deploy.yml`。
- 最近一次 GitHub Actions 自定义 Flutter Web → Vercel CLI 流程失败在 `Link Vercel project`；此前的 Flutter analyze、test、build 步骤均成功。
- 同一提交 `bdde66d` 的 Vercel GitHub App commit status 为 `success`，说明当前同时存在两套重叠部署机制，状态不一致。
- GitHub Pages `https://chrisjiang2016.github.io/PlantStory/` 当前 HTTP 200，但展示的是过时 README，不是 Flutter Demo。
- 历史 Vercel 域名 `https://plant-story-jly2008.vercel.app` 本次无法连接；GitHub API 中记录的 deployment URL 本次也无法直接访问，因此没有完成稳定 Flutter Demo 公网入口验收。
- Railway 后端 `https://plantstory-production.up.railway.app/api/v1/health` 本次返回 404，当前不能视为在线生产后端。

### 6. 仓库安全

- `.env` 已被 `.gitignore` 忽略。
- `credentials.json` 与 `perenual key.txt` 仍在 Git 索引中。
- 公开仓库继续推广前，需要移除跟踪、补 `.gitignore`、检查 Git 历史并轮换相关凭据。

## 三、进度判断

| 维度 | 完成度 | 结论 |
|---|---:|---|
| 后端 V1 功能 | 80% | 10 模块、44 路由和核心业务已实现；测试基线需修复。 |
| Flutter Web 核心产品 | 55% | 四条核心链路完成；其余业务页面和完整 UI 未完成。 |
| 公开 Demo | 90% | 本地代码闭环完整；稳定公网入口未验收。 |
| Docker 本地部署 | 90% | 三服务历史实测通过；本次因 Docker daemon 未运行未复验。 |
| Android/iOS 原生端 | 30% | 原生配置和安全存储代码已接入，真机未验证。 |
| 生产部署与运维 | 35% | 部署物料存在，但公网后端、CI、域名、监控和备份未收口。 |
| 整体 V1 | 65% | 已进入可演示 Beta / 发布工程收口阶段，尚未达到生产发布标准。 |

## 四、当前优先级

### P0：工程基线修复

1. 把后端测试环境迁移到独立 PostgreSQL 测试库或 Testcontainers。
2. 恢复 7 suites / 88 tests 全绿并确保可重复执行。
3. 从 Git 跟踪中移除 `credentials.json`、`perenual key.txt`，检查历史并轮换凭据。

### P1：部署链路收口

1. 在 Vercel GitHub App 与 GitHub Actions + Vercel CLI 中只保留一套部署机制。
2. 恢复并验收一个稳定的 Flutter Demo 公网域名。
3. 恢复 Railway 或替换为新的后端部署，实测 `/api/v1/health` 和核心 API。
4. 更新前端生产 `API_BASE_URL`。

### P1：文档一致性

1. 更新根 README。
2. 更新项目任务进度文档。
3. 修正 GitHub 仓库描述中的“值の物语”错字。
4. 明确 PostgreSQL、Prisma 6.19.3、19 个 Flutter 测试、Docker 和 CI 的真实状态。

### P2：产品功能继续开发

1. CareLogs / Diaries / Favorites / Achievements 页面。
2. Diagnosis / Encyclopedia 页面。
3. Profile/个人中心完整能力。
4. Android/iOS 真机联调、相机权限和安全存储验证。
5. 对象存储、图片生命周期、监控、备份和发布工程。

## 五、2026-08-26 工程基线修复结果

### 1. PostgreSQL 测试基线：已修复

- 新增 `backend/scripts/run-tests.js`，所有 `npm test` / `test:sprint-*` 统一通过该运行器执行。
- 测试不再使用 SQLite `file:` URL，改为强制要求显式的 `TEST_DATABASE_URL`。
- 运行器只接受 `postgresql://` 或 `postgres://`，并将其映射为测试进程的 `DATABASE_URL`。
- 若未设置 `TEST_DATABASE_URL`，命令会立即失败并提示；不会回退到开发数据库，避免 `prisma db push --accept-data-loss` 误操作开发或生产数据。
- `test/setup-env.ts` 已移除 SQLite fallback；新增 `backend/.env.test.example`，说明隔离测试库的配置方式。
- 本机已启动隔离容器 `plant-story-test-db`，数据库为 `plant_story_test`，端口为 `127.0.0.1:5433`，测试用户为 `plant_story_test`。

首次全量验证：

```text
npm test
Test Suites: 7 passed, 7 total
Tests:       88 passed, 88 total
```

补充观察：第二次紧接复跑在 5 个 suite 已通过后发生 Node 异常退出（Windows exit code `-1073740791`），日志未出现业务测试断言失败。首次全绿结果有效；该复跑稳定性现象应在后续 CI/Linux 环境继续观察。

### 2. 仓库安全：当前工作区已治理，历史待确认

- `.gitignore` 已加入 `credentials.json` 和 `perenual key.txt`。
- 两个文件已通过 `git rm --cached` 从 Git 索引移除，文件仍保留在本地，避免中断本地开发。
- 已验证两者当前均被 Git 忽略。
- Git 历史检查显示：两个文件均由初始提交 `f9b4663` 引入，且该提交存在于 `main` 和 `origin/main` 历史中，因此历史远端暴露已确认。

尚未执行的高影响操作：

1. 轮换 Perenual API Key 及 `credentials.json` 中的全部凭据；
2. 使用 `git filter-repo` 或等效方案重写远端 Git 历史；
3. force-push 重写后的 `main` 并通知所有协作者重新克隆或重置分支。

以上操作会使现有提交哈希失效并影响远端协作者，需在用户明确确认后单独执行。

## 六、下一开发起点

> 工程基线的代码修复已完成并完成首次全量验证。下一步先确认并执行密钥轮换与 Git 历史清理；随后收口 Vercel/Railway 部署链路。完成后再继续全量 Flutter 页面和真机开发。
